# Production overrides. 2 api replicas per addendum §13; worker stays at 1
# (worker concurrency is tuned via env, not replicas). Secrets and the TLS
# cert are managed out-of-band by the platform team.

global:
  environment: production

api:
  replicaCount: 2

worker:
  replicaCount: 1

frontend:
  replicaCount: 2

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: autoboard.example.com
      paths:
        - path: /
          pathType: Prefix
          service: frontend
        - path: /api
          pathType: Prefix
          service: api
  tls:
    - hosts:
        - autoboard.example.com
      secretName: autoboard-production-tls

config:
  environment: production
  logLevel: INFO
  corsOrigins:
    - https://autoboard.example.com
  trustedHosts:
    - autoboard.example.com

secrets:
  existingSecret: autoboard-runtime-production
  dev:
    enabled: false

networkPolicy:
  enabled: true
  monitoringNamespace: monitoring
  allowedEgressCidrs:
    - 10.0.0.0/16

podDisruptionBudget:
  enabled: true

migration:
  enabled: true
