{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "autoboard.fullname" . }}
  labels:
    {{- include "autoboard.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
