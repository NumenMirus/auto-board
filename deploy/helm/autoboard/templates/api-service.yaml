apiVersion: v1
kind: Service
metadata:
  name: {{ include "autobreadboard.fullname" . }}-api
  labels:
    {{- include "autobreadboard.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "autobreadboard.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
